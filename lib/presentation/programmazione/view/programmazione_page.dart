import 'package:flutter/material.dart';

class ProgrammazionePage extends StatelessWidget {
  const ProgrammazionePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Programmazione Page'),
      ),
      body: const Center(
        child: Text('Programmazione Page'),
      ),
    );
  }
}

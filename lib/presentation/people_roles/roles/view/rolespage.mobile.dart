import 'package:flutter/material.dart';

class RolesPageMobile extends StatelessWidget {
  const RolesPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MOBILE PAGE FOR ROLES"),
    );
  }
}

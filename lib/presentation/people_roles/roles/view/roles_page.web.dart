import 'package:flutter/material.dart';

class RolesPageWeb extends StatelessWidget {
  const RolesPageWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("WEBPAGE FOR ROLES"),
      ),
    );
  }
}

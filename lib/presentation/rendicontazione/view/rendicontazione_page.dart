import 'package:flutter/material.dart';

class RendicontazionePage extends StatelessWidget {
  const RendicontazionePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rendicontazione Page'),
      ),
      body: const Center(
        child: Text('Rendicontazione Page'),
      ),
    );
  }
}
